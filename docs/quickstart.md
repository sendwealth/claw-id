# CLAW ID - 快速开始指南

## 🚀 5 分钟启动 CLAW ID

### 第一步：启动后端

```bash
cd /home/rowan/clawd/products/claw-id/backend

# 安装依赖
npm install

# 启动服务
npm start
```

**看到这个输出就成功了：**
```
🦞 CLAW ID API running on port 3000
Health check: http://localhost:3000/health
API base: http://localhost:3000/api/v1
```

---

### 第二步：启动前端

**打开新终端：**

```bash
cd /home/rowan/clawd/products/claw-id/frontend

# 安装依赖（如果还没安装）
npm install

# 启动开发服务器
npm run dev
```

**访问：** http://localhost:3001

---

### 第三步：创建第一个智能体

1. 在浏览器打开 http://localhost:3001
2. 输入智能体名称（例如：Marketing Bot）
3. 选择平台（GitHub、Reddit、Discord）
4. 点击"创建智能体"

**你会看到：**
- 智能体 ID
- 自动生成的邮箱（agent-xxx@claw.id）
- 创建时间

---

## 🧪 测试 API

### 方法 1: 使用 cURL

```bash
# 健康检查
curl http://localhost:3000/health

# 创建智能体
curl -X POST http://localhost:3000/api/v1/agents \
  -H "Content-Type: application/json" \
  -d '{"name":"Test Bot","platforms":["github"]}'

# 获取智能体列表
curl http://localhost:3000/api/v1/agents

# 获取平台列表
curl http://localhost:3000/api/v1/platforms
```

### 方法 2: 使用浏览器

直接访问：
- http://localhost:3000/health
- http://localhost:3000/api/v1
- http://localhost:3000/api/v1/platforms

---

## 📁 项目结构

```
products/claw-id/
├── backend/              # 后端服务
│   ├── src/
│   │   ├── app.js       # Express 服务器
│   │   └── services/    # 核心服务
│   ├── package.json
│   └── .env.example
├── frontend/            # 前端界面
│   ├── app/
│   │   └── page.tsx    # 首页
│   └── package.json
└── docs/
    └── api.md          # API 文档
```

---

## ✅ 当前功能

### 已实现 ✅
- [x] 智能体创建 API
- [x] 邮箱自动生成
- [x] Token 加密管理
- [x] 基础 Web 界面
- [x] 完整 API 文档

### 开发中 ⏳
- [ ] 数据库持久化
- [ ] GitHub 自动注册
- [ ] Reddit/Discord 集成
- [ ] 智能体列表页面
- [ ] 审计日志

---

## 🎯 下一步

### 推荐路径

**1. 测试基础功能（现在）**
- 创建几个测试智能体
- 熟悉 API
- 验证邮箱生成

**2. 集成数据库（今晚）**
- 安装 PostgreSQL
- 执行 schema.sql
- 实现数据持久化

**3. 实现 GitHub 注册（明天）**
- 集成 Playwright
- 实现自动注册流程
- 处理验证码

**4. 发布内测版（3 天后）**
- 完善功能
- 邀请内测用户
- 收集反馈

---

## 🔧 常见问题

### Q: 后端启动失败？

**检查：**
```bash
# 确认 Node.js 版本
node --version  # 需要 v18+

# 确认端口未被占用
lsof i:3000
```

### Q: 前端无法连接后端？

**确认：**
- 后端正在运行（http://localhost:3000/health）
- 没有防火墙阻止

### Q: 如何修改端口？

**后端：**
编辑 `backend/.env`：
```
PORT=3001
```

**前端：**
编辑 `frontend/package.json`：
```json
"scripts": {
  "dev": "next dev -p 3002"
}
```

---

## 💡 提示

**开发模式：**
- 后端使用 `npm run dev`（自动重启）
- 前端使用 `npm run dev`（热更新）

**生产模式：**
- 后端：`npm start`
- 前端：`npm run build && npm start`

---

## 📞 需要帮助？

- **文档：** docs/api.md
- **GitHub：** https://github.com/sendwealth/claw-id
- **Discord：** https://discord.com/invite/clawd
- **邮箱：** support@claw.id

---

## 🎉 成功标志

如果看到以下内容，说明一切正常：

✅ 后端运行在 http://localhost:3000
✅ 前端运行在 http://localhost:3001
✅ 可以创建智能体
✅ 邮箱自动生成
✅ API 响应正常

**恭喜！CLAW ID 已经可以使用了！** 🚀

---

*最后更新: 2026-02-28*
*维护者: AI CEO uc*
